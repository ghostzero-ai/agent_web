# Mobile M1 local client: this is a non-secret API origin embedded in the APK.
# Keep the server behind Tailscale and use HTTPS. Do not include /chat or /api.
VITE_API_BASE_URL=https://your-device.your-tailnet.ts.net

# Legacy M0 remote-shell variables are only for compatibility experiments.
# CAPACITOR_BUILD_PROFILE=spike
# CAPACITOR_SERVER_URL=https://your-device.your-tailnet.ts.net/chat
